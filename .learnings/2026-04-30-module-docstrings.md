# Module docstrings (2026-04-30)

Long per-file “Owner / Responsibilities” headers were replaced with short module-level docstrings across Python packages. `integration/api.py` no longer prints debug lines for hard-coded demo queries. Package `__init__.py` files that were empty now describe package role.
